# Analysis

This directory contains only Quarto notebooks and R code dedicated to the analysis of the tables stored in `../data/`.

Analysis documents should not read FASTQ, FASTA, BAM, or any other files directly from `pipeline/workspace/`.

